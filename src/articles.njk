---
layout: base.njk
title: Articles
permalink: /articles/
description: Evidence-based insights on longevity, nutrition, exercise, sleep, and mental health. Practical guidance for living longer and living well.
---

<section class="page-intro">
  <div class="container">
    <span class="eyebrow">Our Library</span>
    <h1>Articles</h1>
    <p class="lead">Evidence-based insights on longevity, nutrition, exercise, sleep, and mental health. Practical guidance for living longer and living well.</p>
  </div>
</section>

<section>
  <div class="container">
    {% if collections.posts.length > 0 %}
    <div class="articles-grid">
      {%- for post in collections.posts | reverse -%}
      <a href="{{ post.url }}" class="article-link">
        <article class="article-card">
          <div class="article-label">
            {% if post.data.tags and post.data.tags[0] %}
              {{ post.data.tags[0] | formatTag }}
            {% else %}
              Article
            {% endif %}
          </div>
          <h2 class="article-title">{{ post.data.title }}</h2>
          {% if post.data.preview %}
            <p class="article-excerpt">{{ post.data.preview | truncate(150) }}</p>
          {% elif post.data.description %}
            <p class="article-excerpt">{{ post.data.description | truncate(150) }}</p>
          {% endif %}
          <div class="article-meta">
            <time datetime="{{ post.date | dateToFormat('yyyy-MM-dd') }}" class="post-date">
              {{ post.date | readableDate }}
            </time>
          </div>
        </article>
      </a>
      {%- endfor -%}
    </div>
    {% else %}
    <div class="container-narrow">
      <div class="no-posts">
        <p class="lead">More articles coming soon – check back for regular updates on living longer and better.</p>
      </div>
    </div>
    {% endif %}
  </div>
</section>
